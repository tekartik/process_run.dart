import 'package:process_run/cmd_run.dart';

/// Run the build_runner command
class PbrCmd extends PubRunCmd {
  /// Run the build_runner command
  PbrCmd(List<String> arguments) : super('build_runner', arguments);
}
