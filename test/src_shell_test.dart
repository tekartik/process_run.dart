import 'package:test/test.dart';

@Deprecated('Dev only, when uncommeing the line debug = devTrue')
bool devTrue = true;
// bool debug = devTrue;
bool debug = false;

void main() {
  group('src_shell', () {});
}
